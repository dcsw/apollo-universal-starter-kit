#import "./XxxxInfo.graphql"

query xxxxs($limit: Int!, $after: Int) {
    xxxxs(limit: $limit, after: $after) {
        totalCount
        edges {
            cursor
            node {
                ...XxxxInfo
            }
        }
        pageInfo {
            endCursor
            hasNextPage
        }
    }
}
