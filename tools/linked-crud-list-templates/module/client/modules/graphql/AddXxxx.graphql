#import "./XxxxInfo.graphql"
#import "../../yyyy/graphql/YyyyInfo.graphql"

mutation addXxxx($input: AddXxxxInput!) {
    addXxxx(input: $input) {
        ...XxxxInfo
        yyyys {
            ...YyyyInfo
        }
    }
}
