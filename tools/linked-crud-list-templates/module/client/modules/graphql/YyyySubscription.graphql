#import "./YyyyInfo.graphql"

subscription onYyyyUpdated($xxxxId: Int!) {
    yyyyUpdated(xxxxId: $xxxxId) {
        mutation
        id
        xxxxId
        node {
            ...YyyyInfo
        }
    }
}
